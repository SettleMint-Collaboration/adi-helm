name: Release Chart

on:
  push:
    branches: [main]
    paths:
      - "adi-stack/**"
  workflow_dispatch:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v4.0.3

      - name: Lint chart
        run: helm lint adi-stack --strict

      - name: Validate example values
        run: |
          echo "Validating chart templates with example values files..."
          for f in adi-stack/examples/*.yaml; do
            echo "Testing $f"
            helm template test ./adi-stack -f "$f" > /dev/null
          done
          echo "All example values files validated successfully"

  release:
    needs: lint
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Set up Helm
        uses: azure/setup-helm@v4
        with:
          version: v4.0.3

      - name: Run chart-releaser
        uses: helm/chart-releaser-action@v1.7.0
        with:
          charts_dir: .
          skip_existing: true
        env:
          CR_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
